/*
 * LMAX Disruptor
 */
plugins {
    id 'asciidoc'
}

group = 'com.lmax'
version = '4.0.0-SNAPSHOT'

wrapper.gradleVersion = '8.12.1'

allprojects {
    defaultTasks 'build'

    group = rootProject.group
    version = rootProject.version

    repositories {
        mavenCentral()
    }
}